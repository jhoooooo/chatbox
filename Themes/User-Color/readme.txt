Each file can be open with notepad (right click > open with > notepad).

-------------------------------------

User Color

-- Every Twitch user can set their own color in chat.
-- This showcase how to use them in Streamlabs chat widget.
-- The variable is {color}.
-- The variable can only be use in html section. Streamlabs only appends them there.
-- Else You can hook the color via js from obj.detail.tag.color