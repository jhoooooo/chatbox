Beta version.

This one uses different animation code from the rest because the code i usually use doesn't work with different padding per message that this theme need. This theme is suitable if you only want to highlight small number of people in the chat (recommended 1 person only thus 'Only You').